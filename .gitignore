/dist/
/build/
/bin/
/metis
/metis-*
!/metis-desktop/
/metis-desktop/metis-desktop
*.test
*.out
.DS_Store
.idea/
.vscode/
coverage.txt
node_modules/
*.tgz
.env
.env.local

# Local signing credentials and other machine-private release material.
# Never force-add this directory; official CI must use repository secrets.
/.private/
/metis-desktop/release-dist-local/

# local dev artifacts (not for public release)
TEST_REPORT.md

# personal deploy script for Ricardo's tencent-cloud server (not for public release)
/install/install-server.sh

# private architecture notes (not for public release)
技术架构文档.md
